package client

import (
	"context"
	"google.golang.org/grpc"
	pb "Block-P/api/connection" // import the connection.proto methods
)

// func for start conexion with other node
func InitiateConnection() {
  // Lógica para establecer una conexión segura con otro nodo.
  // Llama al método RequestConnection y maneja la respuesta.
}
