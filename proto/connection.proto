syntax = "proto3";

option go_package = "./proto";

package proto.connection;

message ConnectionRequest {
  int64 id = 1;
};

message Acknowledge {
  int64 id = 1;
  string message = 2;
}

service ConnectionService {
  rpc RequestConnection (ConnectionRequest) returns (Acknowledge);
}
